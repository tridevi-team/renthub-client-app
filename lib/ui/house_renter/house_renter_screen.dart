import 'package:flutter/material.dart';

class HouseRenterScreen extends StatelessWidget {
  const HouseRenterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
